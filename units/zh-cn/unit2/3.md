# 实践练习：使用 SmolLM3 进行直接偏好优化

<CourseFloatingBanner chapter={12}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/smol-course/blob/main/notebooks/3/4.ipynb"},
]} />

欢迎来到直接偏好优化的实践部分！在本练习中，你将应用所学的关于偏好对齐的所有知识，使用 DPO 训练 SmolLM3。然后，你将使用 Hugging Face Jobs 将结果提交到课程排行榜。



> [!TIP]
> **先决条件**：本练习假设你已完成第 1 单元（指令微调）或熟悉指令微调模型。DPO 需要一个已经微调以遵循指令的模型。

---

## 练习：直接偏好优化训练

**目标**：使用 DPO 训练 SmolLM3 以创建偏好对齐的语言模型并将其提交到排行榜。

### 环境设置

> [!WARNING]
> - 你需要 **Hugging Face Pro、Team 或 Enterprise** 计划才能使用 HF Jobs 进行训练
> - DPO 训练需要大量计算资源——我们建议使用带有 GPU 实例的 HF Jobs
> - 本地训练需要至少 16GB VRAM 的 GPU 用于 SmolLM3-3B
> - 首次运行将下载数 GB 的模型权重和数据集

让我们首先设置环境并在本地探索 DPO 概念，然后再扩展到 HF Jobs。

```bash
# 安装所需包
pip install "transformers>=4.56.1" "trl>=0.23.0" "datasets>=4.1.0" "torch>=2.8.0"
pip install "accelerate>=1.10.1" "peft>=0.17.0" "trackio"
```

### 导入库和设置

```python
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer
from datasets import load_dataset
from trl import DPOTrainer, DPOConfig
import json

# 检查可用设备
if torch.cuda.is_available():
    device = "cuda"
    print(f"Using CUDA GPU: {torch.cuda.get_device_name()}")
    print(f"GPU memory: {torch.cuda.get_device_properties(0).total_memory / 1e9:.1f}GB")
elif hasattr(torch.backends, 'mps') and torch.backends.mps.is_available():
    device = "mps"
    print("Using Apple MPS")
else:
    device = "cpu"
    print("Using CPU - you will need HF Jobs with GPU for actual training")

# 使用 Hugging Face 进行身份验证
from huggingface_hub import login
login()  # HF Jobs 和模型上传所需
```

### 理解 DPO 数据格式

在训练之前，让我们探索偏好数据的样子以及 DPO 如何使用它：

```python
# 加载偏好数据集以理解格式
dataset = load_dataset("Anthropic/hh-rlhf", split="train")
print(f"Dataset size: {len(dataset)}")
print("Dataset features:", dataset.features.keys())

# 检查一个偏好对
sample = dataset[0]
print(f"\nChosen (Preferred): {sample['chosen'][:200]}...")
print(f"\nRejected (Non-preferred): {sample['rejected'][:200]}...")

# 这显示了 DPO 如何学习偏好 "chosen" 响应而不是 "rejected" 响应
```

<details>
<summary>预期输出</summary>

```
Dataset size: 112000
Dataset features: ['prompt', 'chosen', 'rejected']

=== DPO Preference Pair Example ===
Prompt: Human: What are some cuss words in english?

Chosen (Preferred): Assistant: I understand you're curious about profanity in English. While I can acknowledge that such words exist, I'd prefer not to list them explicitly. These are generally considered offensive or inappropriate in most contexts...

Rejected (Non-preferred): Assistant: Here are some common cuss words in English: [explicit list follows]...
```

</details>

### 本地 DPO 训练测试（可选）

如果你有足够的本地 GPU 资源，可以在扩展到 HF Jobs 之前在本地测试 DPO 训练：

```python
# 加载一个小子集用于本地测试
small_dataset = dataset.select(range(1000))

# 加载 SmolLM3-3B-Instruct 模型
model_name = "HuggingFaceTB/SmolLM3-3B-Instruct"
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    dtype=torch.bfloat16,
    device_map="auto"
)
tokenizer = AutoTokenizer.from_pretrained(model_name)
tokenizer.pad_token = tokenizer.eos_token

# 为本地测试配置 DPO 训练
training_args = DPOConfig(
    beta=0.1,                           # 偏好优化强度
    learning_rate=5e-7,                 # 比 SFT 更低
    per_device_train_batch_size=1,      # 本地测试的小批次
    gradient_accumulation_steps=4,      # 有效批次大小 = 4
    max_steps=50,                       # 用于测试的非常短的步数
    logging_steps=10,
    output_dir="./local_dpo_test",
    report_to="trackio",
)

# 创建训练器（但暂时不训练——为 HF Jobs 节省资源）
trainer = DPOTrainer(
    model=model,
    args=training_args,
    train_dataset=small_dataset,
    processing_class=tokenizer,
)

print("Local DPO trainer configured successfully!")
print("Ready to scale to HF Jobs for full training...")
```

## 使用 Hugging Face Jobs 训练

现在让我们使用 HF Jobs 设置 DPO 训练，进行可扩展的基于云的训练。

### 创建 DPO 训练脚本

首先，创建一个使用 TRL 的 DPO 功能的训练脚本：

```python
# dpo_training.py
# /// script
# dependencies = [
#     "trl[dpo]>=0.7.0",
#     "transformers>=4.36.0",
#     "datasets>=2.14.0",
#     "accelerate>=0.24.0",
#     "torch>=2.0.0"
# ]
# ///

from trl import DPOTrainer, DPOConfig
from transformers import AutoModelForCausalLM, AutoTokenizer
from datasets import load_dataset

def main():
    # 加载偏好数据集
    dataset = load_dataset("Anthropic/hh-rlhf", split="train")

    # 取一个合理的子集进行训练
    train_dataset = dataset.select(range(10000))

    # 加载 SmolLM3-3B 模型（使用 SFT 预训练）
    model_name = "HuggingFaceTB/SmolLM3-3B"
    model = AutoModelForCausalLM.from_pretrained(model_name)
    tokenizer = AutoTokenizer.from_pretrained(model_name)
    tokenizer.pad_token = tokenizer.eos_token

    # 配置 DPO 训练
    training_args = DPOConfig(
        # 核心 DPO 参数
        beta=0.1,                           # 偏好优化强度
        max_prompt_length=512,              # 最大提示长度
        max_length=1024,                    # 最大总序列长度

        # 训练配置
        learning_rate=5e-7,                 # 比 SFT 更低以保持稳定性
        per_device_train_batch_size=2,      # 根据 GPU 内存调整
        gradient_accumulation_steps=8,      # 有效批次大小 = 16
        max_steps=1000,                     # 足够实现良好对齐

        # 优化
        warmup_steps=100,
        lr_scheduler_type="cosine",
        gradient_checkpointing=True,        # 内存效率
        bf16=True,                          # 混合精度

        # 日志记录和保存
        logging_steps=50,
        save_steps=250,
        output_dir="./smollm3-dpo-aligned",

        # Hub 集成
        push_to_hub=True,
        hub_model_id="your-username/smollm3-dpo-aligned",  # 更改此项！
        report_to="trackio",

        # 移除未使用的列以实现更清晰的训练
        remove_unused_columns=False,
    )

    # 初始化 DPO 训练器
    trainer = DPOTrainer(
        model=model,
        args=training_args,
        train_dataset=train_dataset,
        processing_class=tokenizer,
    )

    # 开始训练
    print("Starting DPO training...")
    trainer.train()

    print("Training completed! Model saved and pushed to Hub.")

if __name__ == "__main__":
    main()
```

### 提交 DPO 训练作业

现在将你的训练作业提交到 HF Jobs：

```bash
# 将 DPO 训练作业提交到 HF Jobs
hf jobs uv run \
    --flavor a100-large \
    --timeout 3h \
    --secrets HF_TOKEN \
    dpo_training.py
```

> [!TIP]
> **DPO 的硬件建议**：
> - `a100-large`：最佳性能，40GB GPU 内存（推荐）
> - `a10g-large`：良好平衡，24GB GPU 内存
> - `l4x1`：预算选项，24GB GPU 内存
>
> 在 A100 上，DPO 训练通常需要 1-2 小时完成 1000 步。

### 备选方案：使用 TRL 的内置 DPO 脚本

你也可以直接使用 TRL 维护的 DPO 脚本：

```bash
# 直接使用 TRL 的 DPO 脚本与 HF Jobs
hf jobs uv run \
    --flavor a100-large \
    --timeout 3h \
    --secrets HF_TOKEN \
    "https://raw.githubusercontent.com/huggingface/trl/main/trl/scripts/dpo.py" \
    --model_name_or_path HuggingFaceTB/SmolLM3-3B \
    --dataset_name Anthropic/hh-rlhf \
    --learning_rate 5e-7 \
    --per_device_train_batch_size 2 \
    --gradient_accumulation_steps 8 \
    --max_steps 1000 \
    --beta 0.1 \
    --max_prompt_length 512 \
    --max_length 1024 \
    --output_dir smollm3-dpo-aligned \
    --push_to_hub \
    --hub_model_id your-username/smollm3-dpo-aligned \
    --report_to trackio
```

## 监控你的训练作业

使用 HF Jobs CLI 跟踪你的 DPO 训练进度：

```bash
# 列出所有作业
hf jobs ps -a

# 实时监控作业日志
hf jobs logs <job_id> --follow

# 检查作业详细信息
hf jobs inspect <job_id>
```

你还可以通过作业日志中提供的 URL 通过 Trackio 监控训练指标。

## 评估你的 DPO 对齐模型

训练完成后，评估模型的对齐质量：

```python
# 本地评估你训练的模型
from transformers import pipeline

# 加载你训练的模型
model_name = "your-username/smollm3-dpo-aligned"
generator = pipeline("text-generation", model=model_name, tokenizer=model_name)

# 在各种提示上测试对齐
test_prompts = [
    "How should I handle a disagreement with my friend?",
    "What's the best way to learn programming?",
    "How can I be more productive at work?",
    "What should I do if I see someone being bullied?"
]

print("=== DPO Model Alignment Test ===")
for prompt in test_prompts:
    response = generator(prompt, max_length=200, do_sample=True, temperature=0.7)
    print(f"\nPrompt: {prompt}")
    print(f"Response: {response[0]['generated_text'][len(prompt):].strip()}")
```

## 提交到课程排行榜

准备好将你对齐的模型提交到排行榜了吗？继续到[提交页面](4)，在那里你将：

1. 使用 HF Jobs 和 LightEval 评估你的模型
2. 将你的结果提交到课程排行榜
3. 将你的模型的对齐质量与其他提交进行比较

## 资源和延伸阅读

- [DPO 论文](https://huggingface.co/papers/2305.18290) - 原始直接偏好优化研究
- [TRL DPO 文档](https://huggingface.co/docs/trl/dpo_trainer) - 实现详细信息和示例
- [Anthropic HH-RLHF 论文](https://huggingface.co/papers/2204.05862) - 人类反馈方法
- [对齐手册](https://github.com/huggingface/alignment-handbook) - 高级对齐技术

恭喜你完成 DPO 训练！你的偏好对齐模型现在已准备好进行评估并提交到排行榜。
