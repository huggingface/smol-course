# 实践练习：微调 SmolVLM2-2.2B-Instruct

<CourseFloatingBanner chapter={10}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/smol-course/blob/main/notebooks/4/4.ipynb"},
]} />

欢迎来到实践部分！在这里，你将使用 **HuggingFaceTB/SmolVLM2-2.2B-Instruct** 实践你学到的关于视觉语言模型 (VLM) 的所有知识。
练习从基础概念逐步发展到高级技术，帮助你获得真实世界的实践经验。

## 学习目标

在这些练习结束时，你将能够：

- **使用 VLM 数据集**：探索和准备 **HuggingFaceM4/ChartQA**。
- **优化训练**：应用**量化**和 **PEFT** 以实现高效微调。
- **实践微调模型**：使用 Python API 和 CLI 工具训练 **HuggingFaceTB/SmolVLM2-2.2B-Instruct**。
- **为 TRL 适应数据集**：准备 VLM 数据集以与 TRL 工作流无缝集成。
- **转向生产**：了解如何扩展和管理 VLM 的**生产就绪微调工作流**。

### 练习 1：探索 `SmolVLM2-2.2B-Instruct`

**目标**：熟悉 `SmolVLM2-2.2B-Instruct` 模型并使用数据集中的样本评估模型。

### 环境设置

> [!WARNING]
> - 你需要至少 8GB VRAM 的 GPU 进行训练。CPU/MPS 可以运行格式化和数据集探索，但训练更大的模型可能会失败。
> - 首次运行将下载数 GB 的模型权重；确保有 15GB+ 的可用磁盘空间和稳定的连接。
> - 如果你需要访问私有仓库，请通过 `login()` 使用 Hugging Face Hub 进行身份验证。

首先，安装所需的库：`transformers`、`datasets`、`trl`、`huggingface_hub` 和 `trackio`。
这些包提供了处理模型、数据集和 Hugging Face Hub 的工具。


```bash
# 安装所需包（在 Colab 或你的环境中运行）
pip install transformers datasets trl huggingface_hub trackio num2words==0.5.14
```

### 导入依赖项

现在，导入我们将使用的主要依赖项：

```python
# 导入依赖项

import torch
import os
from transformers import AutoProcessor, AutoModelForImageTextToText, BitsAndBytesConfig
from transformers.image_utils import load_image
```

### 加载模型和处理器

#### 1. 选择设备

我们首先选择模型将运行的设备。它可以是 GPU (`cuda`)、Apple Silicon (`mps`) 或作为后备的 CPU。

```python
device = (
    "cuda"
    if torch.cuda.is_available()
    else "mps" if torch.backends.mps.is_available() else "cpu"
)
```

#### 2. 使用 Hugging Face 进行身份验证

要使用私有模型或**将你微调的模型推送到 Hub**（正如我们将在本练习中所做的那样），你需要使用你的 Hugging Face 账户进行身份验证。

> [!TIP]
> 你可以从你个人资料中的 [Hugging Face tokens 页面](https://huggingface.co/settings/tokens)创建并复制你的访问 token。

```python
from huggingface_hub import login
login()
```

#### 3. 加载模型和处理器

最后，我们加载 **HuggingFaceTB/SmolVLM2-2.2B-Instruct** 模型。
`AutoProcessor` 也在这里初始化——它确保文本和图像在传递给模型之前都得到正确预处理。

```python
model_name = "HuggingFaceTB/SmolVLM2-2.2B-Instruct"
model = AutoModelForImageTextToText.from_pretrained(
    model_name,
    dtype=torch.bfloat16,
).to(device)

processor = AutoProcessor.from_pretrained(model_name)
```

### 探索数据集

在这一步中，我们加载 **ChartQA** 数据集的一个小子集——训练和验证拆分的仅 10%——以保持练习快速且易于学习。

然后，我们使用 `matplotlib` 显示其中一个图表图像，以获得模型输入的视觉感受。
此外，我们打印相应的查询和标签，以便你可以完全理解数据集结构和模型将处理的任务类型。

<iframe
  src="https://huggingface.co/datasets/HuggingFaceM4/ChartQA/embed/viewer/default/train"
  frameborder="0"
  width="100%"
  height="560px"
></iframe>

```python
from datasets import load_dataset
import matplotlib.pyplot as plt

train_dataset, eval_dataset = load_dataset("HuggingFaceM4/ChartQA", split=["train[:10%]", "val[:10%]"])
example = train_dataset[1]
image = load_image(example["image"])

print(example["query"])
print(example["label"][0])
```
<details>
<summary>输出</summary>

```text
How many values are below 40 in Unfavorable graph?
6
```

</details>

```python
plt.imshow(image)
plt.axis("off")
plt.title("Sample Chart Image")
plt.show()
```

![Model output](https://huggingface.co/datasets/smol-course/images/resolve/main/model_output.png)

### 构建聊天式提示

我们创建一个**聊天消息列表**，其中包括用户查询和图像。

使用 `processor.apply_chat_template`，我们将其转换为模型期望的确切输入格式。

```python
# 定义聊天式提示
messages = [
    {"role": "user", "content": [
        {"type": "image", "image": image},
        {"type": "text", "text": example["query"]},
    ]}
]

# 应用聊天模板
chat_prompt = processor.apply_chat_template(
      messages, add_generation_prompt=True\
)
print(chat_prompt)
```

<details>
<summary>输出</summary>

```text
<|im_start|>User:<image>How many values are below 40 in Unfavorable graph?<end_of_utterance>
Assistant:
```

</details>

### 运行推理

我们将聊天提示和图像分词为张量，然后使用模型生成响应。
最后，我们将输出 token 解码回文本。

```python
# 分词输入
inputs = processor(images=[image], text=chat_prompt, return_tensors="pt").to(device)

# 生成模型输出
with torch.no_grad():
    output = model.generate(**inputs, max_new_tokens=20)

# 修剪生成的 id 以删除输入 id
trimmed_generated_ids = [out_ids[len(in_ids) :] for in_ids, out_ids in zip(inputs.input_ids, output)]

# 解码输出文本
output_text = processor.batch_decode(
    trimmed_generated_ids, skip_special_tokens=True, clean_up_tokenization_spaces=False
)
print(output_text[0])
```
<details>
<summary>输出</summary>

```text
 3.
```

</details>

模型生成了一个响应，但不完全正确。通过一些微调可以改进。现在我们已经了解了如何使用 **SmolVLM2-2.2B-Instruct** 构建提示和生成响应，是时候学习如何使用 **LoRA（低秩适应）**有效地适应和**微调**模型了。这种方法允许使用更少的资源训练大型模型，并为特定下游任务准备模型。


## 练习 2：使用 LoRA 微调模型

在本练习中，我们将应用 **LoRA（低秩适应）**来有效微调我们的视觉-语言模型。

LoRA 通过向现有模型层注入**可训练的低秩矩阵**来工作，使**大型模型能够以显著更少的可训练参数进行微调**。
这种方法减少了内存使用并加速了训练，同时保持高性能。

```python
system_message = """You are a Vision Language Model specialized in interpreting visual data from chart images.
Your task is to analyze the provided chart image and respond to queries with concise answers, usually a single word, number, or short phrase.
The charts include a variety of types (e.g., line charts, bar charts) and contain colors, labels, and text.
Focus on delivering accurate, succinct answers based on the visual information. Avoid additional explanation unless absolutely necessary."""
```

我们将把数据集格式化为**聊天机器人式结构**，其中每个示例包括：

- 定义助手角色的**系统消息**
- **图表图像**
- **用户查询**
- **预期答案**

这是 `SFTTrainer` 期望的格式，包括 `images` 和 `messages` 列。
你可以在[文档](https://huggingface.co/docs/trl/en/sft_trainer#training-vision-language-models)中了解更多关于为 VLM 后训练准备数据集的信息。

---

### 格式化数据集

第一步是为 VLM 训练构建数据集。
我们将定义一个**系统消息**，指示模型充当**图表分析专家**，提供关于图表图像的**简洁、准确的答案**。


```python
def format_data(sample):
    return {
        "images": [sample["image"]],
        "messages": [
            {
                "role": "system",
                "content": [{"type": "text", "text": system_message}],
            },
            {
                "role": "user",
                "content": [
                    {
                        "type": "image",
                        "image": sample["image"],
                    },
                    {
                        "type": "text",
                        "text": sample["query"],
                    },
                ],
            },
            {
                "role": "assistant",
                "content": [{"type": "text", "text": sample["label"][0]}],
            },
        ],
    }
```

现在，让我们使用聊天机器人结构格式化数据。这将为模型设置交互。

```python
train_dataset = [format_data(sample) for sample in train_dataset]
eval_dataset = [format_data(sample) for sample in eval_dataset]
```

### 配置 LoRA

在这里，我们定义一个 `LoraConfig`：
- `r` 和 `lora_alpha` 控制适应矩阵的秩和缩放。
- `target_modules` 指定要适应的模型部分。
- `task_type` 设置为因果语言建模。

然后，我们使用 `get_peft_model` 将 LoRA 应用于基础模型，并打印可训练参数以验证适应。

```python
from peft import LoraConfig, get_peft_model

# 配置 LoRA
peft_config = LoraConfig(
    lora_alpha=16,
    lora_dropout=0.05,
    r=8,
    target_modules=["q_proj", "v_proj"],
    task_type="CAUSAL_LM",
)

# 应用 PEFT 模型适应
peft_model = get_peft_model(model, peft_config)

# 打印可训练参数
peft_model.print_trainable_parameters()
```

### 设置训练器

我们使用 `SFTConfig` 从 `trl` 配置 **SFTTrainer**：
- `num_train_epochs`、`batch_size` 和 `gradient_accumulation_steps` 控制训练循环。
- `gradient_checkpointing` 和 `bf16` 优化内存和速度。
- `learning_rate` 管理优化。
- `train_dataset` 和 `eval_dataset` 与你的数据集对齐。

这准备训练器使用 PEFT/LoRA 处理微调。

```python
from trl import SFTConfig, SFTTrainer

# 使用 SFTConfig 配置训练参数
training_args = SFTConfig(
    output_dir="smol-course-smolvlm2-2.2b-instruct-trl-sft-ChartQA",
    num_train_epochs=1,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,
    learning_rate=1e-4,
    logging_steps=25,
    save_strategy="steps",
    save_steps=25,
    optim="adamw_torch_fused",
    bf16=True,
    push_to_hub=True,
    report_to="trackio",
    max_length=None,
)

# 初始化训练器
trainer = SFTTrainer(
    model=model,
    args=training_args,
    train_dataset=train_dataset,
    eval_dataset=eval_dataset,
    peft_config=peft_config,
)

# 将 SFTTrainer 参数与你选择的数据集对齐。
```

### 训练和保存模型

现在我们运行训练循环：
1. `trainer.train()` 使用 LoRA 开始微调。
2. `trainer.save_model()` 存储本地训练的模型。

这一步确保模型准备好用于下游任务，只需最少的额外参数。

```python
# 训练模型
trainer.train()

# 保存模型
trainer.save_model(training_args.output_dir)
```

有了基于 Python 的微调和 LoRA 的基础，我们现在可以使用 **TRL CLI** 将此工作流转移到**生产环境**。这种方法使你可以自动化微调并创建可重现的流程，而无需编写完整的 Python 脚本。

## 练习 3：使用 TRL CLI 的生产工作流

在之前的练习中，我们专注于使用 Python API 微调 **SmolVLM2-2.2B-Instruct**，探索数据集准备和生成聊天式提示。

在本练习中，我们将演示如何使用 **TRL CLI** 执行微调，这是生产环境中的常见工作流。CLI 允许你运行实验和管理训练，而无需编写 Python 脚本。如果你想复习，我们之前在[这里](../unit1/4#exercise-4-production-workflow-with-trl-cli)介绍了这个工具，相同的概念和故障排除技巧适用。

TRL CLI 利用与 Python API 相同的逻辑和配置选项，但通过简单的命令行界面呈现它们。这意味着你可以在单个命令中定义所有内容——从模型和数据集到训练超参数和输出位置。

下面的示例显示了如何在 **trl-lib/llava-instruct-mix** 数据集上微调 **SmolVLM2-2.2B-Instruct**，使用 LoRA 进行参数高效微调，使用混合精度进行更快训练，以及可选的推送到 Hub 以共享你的模型。现在数据集不同了。

我们在这里使用不同的数据集，因为它已经以早先讨论的预期 VLM 结构格式化。

- `--model_name_or_path` 指定要微调的基础模型。
- `--dataset_name` 和 `--dataset_config` 定义数据集和子集。
- `--output_dir` 设置保存微调模型的本地目录。
- `--per_device_train_batch_size` 和 `--gradient_accumulation_steps` 控制有效批次大小和内存使用。
- `--learning_rate`、`--num_train_epochs` 和 `--max_length` 定义核心训练超参数。
- `--bf16` 在兼容的 GPU 上启用混合精度以实现更快、更节省内存的训练。
- `--push_to_hub` 和 `--hub_model_id` 允许将训练的模型自动上传到你的 Hugging Face Hub 仓库。

使用 TRL CLI 在功能上等同于编写完整的 Python 训练脚本，但配置更快，更易于重现，非常适合生产流程或自动化训练工作流。


```bash
trl sft \
    --model_name_or_path HuggingFaceTB/SmolVLM2-2.2B-Instruct \
    --dataset_name trl-lib/llava-instruct-mix \
    --output_dir ./smolvln-instruct-sft-cli \
    --per_device_train_batch_size 1 \
    --gradient_accumulation_steps 16 \
    --learning_rate 2e-4 \
    --num_train_epochs 3 \
    --max_length -1 \
    --logging_steps 5 \
    --save_steps 100 \
    --warmup_steps 50 \
    --bf16 True \
    --push_to_hub \
    --hub_model_id your-username/smolvlm2-2.2b-instruct-sft-cli
```

## 练习 4：使用 Hugging Face Jobs 训练

在[第 1 单元](../unit1/5)中，我们介绍了 **Hugging Face Jobs (HF Jobs)** 并演示了如何使用这种托管云服务微调模型。
HF Jobs 提供**完全托管的基础设施**来训练模型，无需设置 GPU、管理依赖项或在本地配置环境。这对于 **SFT 训练**特别有用，因为它可能既耗费资源又耗时。

遵循相同的方法，我们可以使用 HF Jobs 微调我们的视觉-语言模型 (VLM)。
如果需要，请回到第 1 单元以刷新你对 HF Jobs 及其工作流的理解。

以下是如何使用 **TRL 维护的 SFT 脚本**启动训练作业的示例：


```bash
# 直接使用 TRL 维护的 SFT 脚本
hf jobs uv run \
    --flavor a10g-large \
    --timeout 2h \
    --secrets HF_TOKEN \
    --with num2words==0.5.14 \
    "https://raw.githubusercontent.com/huggingface/trl/main/trl/scripts/sft.py" \
    --model_name_or_path HuggingFaceTB/SmolVLM2-2.2B-Instruct \
    --dataset_name trl-lib/llava-instruct-mix\
    --learning_rate 5e-5 \
    --per_device_train_batch_size 4 \
    --max_length -1 \
    --max_steps 1000 \
    --output_dir smolvlm2-2.2b-instruct-sft-jobs \
    --push_to_hub \
    --hub_model_id your-username/smolvlm2-2.2b-instruct-sft-jobs \
    --report_to trackio
```

启动作业后，HF Jobs 将在云中处理**整个训练过程**。你可以直接从 Hugging Face Hub 监控进度、查看日志和跟踪指标。

作业完成后：

- **微调模型**将在你指定的 `output_dir` 中可用。
- 如果使用了 `--push_to_hub`，模型也将**从你的 Hugging Face 账户访问**，准备好进行推理或进一步微调。
- 你可以通过重新运行或修改作业配置轻松**恢复、复制或扩展**训练。

这个工作流消除了管理本地资源的开销，使你可以专注于**模型实验和评估**。

## 测试你的知识

你已经完成了本单元——做得很好！
现在通过参加[测验](https://huggingface.co/spaces/smol-course/unit_4_quiz)来测试你的学习成果。

## 延伸学习资源

以下是一些有用的资源，可以加深你的理解并继续尝试视觉语言模型和 TRL 工作流：

- **[TRL 文档](https://huggingface.co/docs/trl/)** – 使用 TRL 的完整参考，包括 Python API 和 CLI。
- **[HuggingFaceTB/SmolVLM2-2.2B-Instruct 模型卡](https://huggingface.co/HuggingFaceTB/SmolVLM2-2.2B-Instruct)** – 有关模型架构、训练和使用的详细信息。
- **[HuggingFaceM4/ChartQA 数据集](https://huggingface.co/datasets/HuggingFaceM4/ChartQA)** – 用于训练和微调 VLM 的数据集。
- **[Hugging Face Hub](https://huggingface.co/)** – 共享你微调的模型并发现社区模型的平台。
- **[Hugging Face Discord 社区](https://discord.gg/huggingface)** – 加入社区进行讨论、支持和故障排除。
