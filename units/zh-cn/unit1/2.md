# 对话模板

对话模板 (Chat Templates) 是指令微调的基础——它们为语言模型、用户和外部工具之间的交互提供一致的格式。可以把它们看作是"语法"，教导模型如何理解对话、区分不同的说话者并做出适当的响应。

## 基础模型 vs 指令模型

首先，我们需要理解基础模型和指令模型之间的区别。这对于有效的微调至关重要。

**基础模型 (`SmolLM3-3B-Base`)**：在原始文本上训练以预测下一个词元。如果你给它"The weather today is"，它可能会继续"sunny and warm"或任何合理的后续内容。

**指令模型 (`SmolLM3-3B`)**：经过微调以遵循指令和参与对话。如果你问"What's the weather like?"，它会将此理解为需要作为新消息进行响应的问题。

### 转换过程

从基础模型到指令模型的过程包括：

- 对话模板：语言模型、用户和外部工具之间交互的结构化格式。
- 监督微调：用于训练模型生成适当响应的技术。

SmolLM3 使用 **ChatML（Chat Markup Language）** 格式，由于其清晰性和灵活性，它已成为行业标准。

> [!TIP]
> 在下一章中，我们将讨论偏好对齐。这是一种允许你微调模型以生成人类偏好响应的技术。

## Pipeline 使用：自动化对话处理

使用开源大语言模型最简单的方法是使用 🤗 Transformers 中的 `pipeline` 抽象。它无缝处理对话模板，使得使用对话模型变得容易，无需手动管理模板。事实上，你甚至不需要了解对话模板格式。

```python
from transformers import pipeline

# 初始化 pipeline
pipe = pipeline("text-generation", "HuggingFaceTB/SmolLM3-3B", device_map="auto")

# 定义你的对话
messages = [
    {"role": "system", "content": "You are a friendly chatbot who always responds in the style of a pirate"},
    {"role": "user", "content": "How many helicopters can a human eat in one sitting?"},
]

# 生成响应 - pipeline 自动处理对话模板
response = pipe(messages, max_new_tokens=128, temperature=0.7)
print(response[0]['generated_text'][-1])  # 打印助手的响应
```

**输出：**
```python
{
    'role': 'assistant',
    'content': "Matey, I'm afraid I must inform ye that humans cannot eat helicopters. Helicopters are not food, they are flying machines. Food is meant to be eaten, like a hearty plate o' grog, a savory bowl o' stew, or a delicious loaf o' bread. But helicopters, they be for transportin' and movin' around, not for eatin'. So, I'd say none, me hearties. None at all."
}
```

在这个例子中，pipeline 自动：

- 根据 Hugging Face Hub 仓库中模型的分词器 (Tokenizer) 配置应用正确的对话模板
- 根据模型的分词器配置自动处理分词和生成
- 返回带有角色信息的结构化输出
- 管理生成参数和停止条件

### 高级 Pipeline 使用

我们可以通过向 pipeline 抽象传递 `generation_config` 字典来对生成过程进行细粒度控制。

```python
# 配置生成参数
generation_config = {
    "max_new_tokens": 200,
    "temperature": 0.8,
    "do_sample": True,
    "top_p": 0.9,
    "repetition_penalty": 1.1
}

# 多轮对话
conversation = [
    {"role": "system", "content": "You are a helpful math tutor."},
    {"role": "user", "content": "Can you help me with calculus?"},
]

# 生成第一个响应
response = pipe(conversation, **generation_config)
conversation = response[0]['generated_text']

# 继续对话
conversation.append({"role": "user", "content": "What is a derivative?"})
response = pipe(conversation, **generation_config)

print("Final conversation:")
for message in response[0]['generated_text']:
    print(f"{message['role']}: {message['content']}")
```

## 理解 SmolLM3 的对话模板

现在我们理解了使用对话模型进行基本推理，让我们深入了解对话模板格式。SmolLM3 使用一个通用的对话模板来处理多种对话类型。让我们看看它是如何工作的：

如果你想动手探索对话模板，可以试试对话模板练习场：

<iframe
	src="https://huggingfacejs-chat-template-playground.hf.space"
	frameborder="0"
	width="850"
	height="450"
></iframe>


### ChatML 格式结构

SmolLM3 使用 ChatML 格式，具有清晰划分对话不同部分的特殊标记。例如，系统消息用 `<|im_start|>system` 和 `<|im_end|>` 标记。

```xml
<|im_start|>system
You are a helpful assistant focused on technical topics.<|im_end|>
<|im_start|>user
Hi there!<|im_end|>
<|im_start|>assistant
Nice to meet you!<|im_end|>
<|im_start|>user
Can I ask a question?<|im_end|>
<|im_start|>assistant
```

**关键组件：**
- `<|im_start|>` 和 `<|im_end|>`：标记每条消息开始和结束的特殊标记
- 角色：`system`、`user`、`assistant`（以及用于函数调用的 `tool`）
- 内容：角色声明和 `<|im_end|>` 之间的实际消息文本

### 双模式推理支持

SmolLM3 是一类可以推理或不推理的新模型。它通过特殊格式和参数启用此功能。如果参数设置为 `think`，模型将显示其推理过程。这通过 `thinking` 标记传达给模型。

**标准模式 (`no_think`)：**
```xml
<|im_start|>user
What is 15 × 24?<|im_end|>
<|im_start|>assistant
15 × 24 = 360<|im_end|>
```

**思考模式 (`think`)：**
```xml
<|im_start|>user
What is 15 × 24?<|im_end|>
<|im_start|>assistant
<|thinking|>
I need to multiply 15 by 24. Let me break this down:
15 × 24 = 15 × (20 + 4) = (15 × 20) + (15 × 4) = 300 + 60 = 360
</|thinking|>

15 × 24 = 360<|im_end|>
```

这种双模式能力使 SmolLM3 能够在需要时展示其推理过程，使其非常适合结合复杂和简单任务。

## 在代码中使用 SmolLM3 对话模板

`transformers` 库通过分词器自动处理对话模板格式。这意味着你只需要正确构建消息，库会处理特殊标记格式。以下是如何使用 SmolLM3 的对话模板：

```python
from transformers import AutoTokenizer

# 加载 SmolLM3 的分词器
tokenizer = AutoTokenizer.from_pretrained("HuggingFaceTB/SmolLM3-3B")

# 将对话构建为消息字典列表
messages = [
    {"role": "system", "content": "You are a helpful assistant focused on technical topics."},
    {"role": "user", "content": "Can you explain what a chat template is?"},
    {"role": "assistant", "content": "A chat template structures conversations between users and AI models by providing a consistent format that helps the model understand different roles and maintain context."}
]

# 应用对话模板
formatted_chat = tokenizer.apply_chat_template(
    messages,
    tokenize=False,  # 返回字符串而不是词元
    add_generation_prompt=True  # 为下一个助手响应添加提示词
)

print(formatted_chat)
```

**输出：**
```xml
<|im_start|>system
You are a helpful assistant focused on technical topics.<|im_end|>
<|im_start|>user
Can you explain what a chat template is?<|im_end|>
<|im_start|>assistant
A chat template structures conversations between users and AI models by providing a consistent format that helps the model understand different roles and maintain context.<|im_end|>
<|im_start|>assistant
```

### 理解消息结构

对话中的每条消息都遵循简单的字典格式：

- `role`：标识说话者（`system`、`user`、`assistant` 或 `tool`）。
- `content`：实际的消息内容。

**消息类型：**

1. **系统消息**：为整个对话设置行为和上下文
2. **用户消息**：来自人类用户的问题、请求或陈述
3. **助手消息**：来自 AI 模型的响应
4. **工具消息**：函数调用的结果（用于高级用例）

## 系统消息：设置上下文

系统消息对于控制 SmolLM3 的行为至关重要。它们充当持久性指令，影响所有后续交互。要创建系统消息，你可以使用 `system` 角色和 `content` 键：

```python
# 专业助手
system_message = {
    "role": "system",
    "content": "You are a professional customer service agent. Always be polite, clear, and helpful."
}

# 技术专家
system_message = {
    "role": "system",
    "content": "You are a senior software engineer. Provide detailed technical explanations with code examples when appropriate."
}

# 创意助手
system_message = {
    "role": "system",
    "content": "You are a creative writing assistant. Help users craft engaging stories and provide constructive feedback."
}
```

> [!TIP]
> 系统消息对模型的行为有重大影响。它们是对话中的第一条消息，为整个对话设定基调。它们应该具体、设定边界、提供上下文并使用示例。

## 多轮对话

SmolLM3 可以在多个对话轮次中保持上下文。每条消息都建立在之前的上下文之上。例如，以下代码创建了与一个有用的编程导师的对话：

```python
conversation = [
    {"role": "system", "content": "You are a helpful programming tutor."},
    {"role": "user", "content": "I'm learning Python. Can you explain functions?"},
    {"role": "assistant", "content": "Functions in Python are reusable blocks of code that perform specific tasks. They're defined using the 'def' keyword."},
    {"role": "user", "content": "Can you show me an example?"},
    {"role": "assistant", "content": "Sure! Here's a simple function:\n\n```python\ndef greet(name):\n    return f'Hello, {name}!'\n\nresult = greet('Alice')\nprint(result)  # Output: Hello, Alice!\n```"},
    {"role": "user", "content": "How do I make it return multiple values?"},
]
```

## 生成提示词：控制模型行为

对话模板中最重要的概念之一是**生成提示词** (Generation Prompt)。这告诉模型何时应该开始生成响应而不是继续现有文本。

### 理解 add_generation_prompt

`add_generation_prompt` 参数控制模板是否添加指示机器人响应开始的标记：

```python
from transformers import AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained("HuggingFaceTB/SmolLM3-3B")

messages = [
    {"role": "user", "content": "Hi there!"},
    {"role": "assistant", "content": "Nice to meet you!"},
    {"role": "user", "content": "Can I ask a question?"}
]

# 不带生成提示词 - 用于完整对话
formatted_without = tokenizer.apply_chat_template(
    messages,
    tokenize=False,
    add_generation_prompt=False
)

print("Without generation prompt:")
print(formatted_without)
print("\n" + "="*50 + "\n")

# 带生成提示词 - 用于推理
formatted_with = tokenizer.apply_chat_template(
    messages,
    tokenize=False,
    add_generation_prompt=True
)

print("With generation prompt:")
print(formatted_with)
```

**输出：**
```xml
Without generation prompt:
<|im_start|>user
Hi there!<|im_end|>
<|im_start|>assistant
Nice to meet you!<|im_end|>
<|im_start|>user
Can I ask a question?<|im_end|>

==================================================

With generation prompt:
<|im_start|>user
Hi there!<|im_end|>
<|im_start|>assistant
Nice to meet you!<|im_end|>
<|im_start|>user
Can I ask a question?<|im_end|>
<|im_start|>assistant
```

生成提示词确保当模型生成文本时，它将写入机器人响应，而不是做一些意外的事情，比如继续用户的消息。

### 何时使用生成提示词

- 用于推理：当你希望模型生成响应时，使用 `add_generation_prompt=True`。
- 用于训练：在准备包含完整对话的训练数据时，使用 `add_generation_prompt=False`。
- 用于评估：使用 `add_generation_prompt=True` 来测试模型响应。

## 继续最后的消息：高级响应控制

`continue_final_message` 参数允许你让模型继续对话中的最后一条消息，而不是开始新的消息。这对于"预填充"响应或确保特定输出格式特别有用。

### 基本示例

```python
# 预填充 JSON 响应
chat = [
    {"role": "user", "content": "Can you format the answer in JSON?"},
    {"role": "assistant", "content": '{"name": "'},
]

# 继续最后的消息
formatted_chat = tokenizer.apply_chat_template(
    chat,
    tokenize=False,
    continue_final_message=True
)

print("Continuing final message:")
print(formatted_chat)
print("\n" + "="*50 + "\n")

# 与开始新消息比较
formatted_new = tokenizer.apply_chat_template(
    chat,
    tokenize=False,
    add_generation_prompt=True
)

print("Starting new message:")
print(formatted_new)
```

**输出：**
```xml
Continuing final message:
<|im_start|>user
Can you format the answer in JSON?<|im_end|>
<|im_start|>assistant
{"name": "

==================================================

Starting new message:
<|im_start|>user
Can you format the answer in JSON?<|im_end|>
<|im_start|>assistant
{"name": "<|im_end|>
<|im_start|>assistant
```

### 实际应用

**1. 结构化输出生成：**

```python
# 强制模型完成特定格式
messages = [
    {"role": "system", "content": "You are a helpful assistant that always responds in JSON format."},
    {"role": "user", "content": "What's the capital of France?"},
    {"role": "assistant", "content": '{\n  "question": "What\'s the capital of France?",\n  "answer": "'}
]

# 模型将仅继续答案，保持 JSON 结构
```

**2. 代码补全：**

```python
# 引导模型完成代码
messages = [
    {"role": "user", "content": "Write a Python function to calculate factorial"},
    {"role": "assistant", "content": "def factorial(n):\n    if n == 0:\n        return 1\n    else:\n        return n * "}
]

# 模型将完成递归调用
```

**3. 逐步推理：**

```python
# 引导模型进行结构化思考
messages = [
    {"role": "user", "content": "Solve: 2x + 5 = 13"},
    {"role": "assistant", "content": "Let me solve this step by step:\n\nStep 1: "}
]

# 模型将继续第一步
```

### 重要说明

- 你不能同时使用 `add_generation_prompt=True` 和 `continue_final_message=True`
- 使用 `continue_final_message=True` 时，最后一条消息必须具有 "assistant" 角色
- 此功能从最后一条消息中删除序列结束标记

## 使用推理模式

SmolLM3 的双模式推理可以通过特殊格式控制：

### 标准模式 vs 思考模式

```python
# 标准模式 - 直接答案
standard_messages = [
    {"role": "user", "content": "What is 15 × 24?"},
    {"role": "assistant", "content": "15 × 24 = 360"}
]

# 思考模式 - 显示推理过程
thinking_messages = [
    {"role": "user", "content": "What is 15 × 24?"},
    {"role": "assistant", "content": "<|thinking|>\nI need to multiply 15 by 24. Let me break this down:\n15 × 24 = 15 × (20 + 4) = (15 × 20) + (15 × 4) = 300 + 60 = 360\n</|thinking|>\n\n15 × 24 = 360"}
]

# 应用模板
standard_formatted = tokenizer.apply_chat_template(standard_messages, tokenize=False)
thinking_formatted = tokenizer.apply_chat_template(thinking_messages, tokenize=False)

print("Standard mode:")
print(standard_formatted)
print("\nThinking mode:")
print(thinking_formatted)
```

### 使用思考模式训练

在准备带有思考模式的数据集时，你可以控制是否包含推理：

```python
def create_thinking_example(question, answer, reasoning=None):
    """创建带有可选思考的训练示例"""
    if reasoning:
        assistant_content = f"<|thinking|>\n{reasoning}\n</|thinking|>\n\n{answer}"
    else:
        assistant_content = answer

    return [
        {"role": "user", "content": question},
        {"role": "assistant", "content": assistant_content}
    ]

# 使用示例
math_example = create_thinking_example(
    question="What is the derivative of x²?",
    answer="The derivative of x² is 2x",
    reasoning="Using the power rule: d/dx(x^n) = n·x^(n-1)\nFor x²: n=2, so d/dx(x²) = 2·x^(2-1) = 2x"
)
```

## 工具使用和函数调用

现代对话模板支持工具使用和函数调用。以下是如何在 SmolLM3 中使用工具：

### 定义工具

```python
# 定义可用工具
tools = [
    {
        "type": "function",
        "function": {
            "name": "get_weather",
            "description": "Get the current weather for a location",
            "parameters": {
                "type": "object",
                "properties": {
                    "location": {
                        "type": "string",
                        "description": "The city and state, e.g. San Francisco, CA"
                    },
                    "unit": {
                        "type": "string",
                        "enum": ["celsius", "fahrenheit"],
                        "description": "The temperature unit"
                    }
                },
                "required": ["location"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "calculate",
            "description": "Perform mathematical calculations",
            "parameters": {
                "type": "object",
                "properties": {
                    "expression": {
                        "type": "string",
                        "description": "Mathematical expression to evaluate"
                    }
                },
                "required": ["expression"]
            }
        }
    }
]
```

### 带工具的对话模板

```python
# 使用工具的对话
messages = [
    {"role": "system", "content": "You are a helpful assistant with access to tools."},
    {"role": "user", "content": "What's the weather like in Paris?"},
    {
        "role": "assistant",
        "content": "I'll check the weather in Paris for you.",
        "tool_calls": [
            {
                "id": "call_1",
                "type": "function",
                "function": {
                    "name": "get_weather",
                    "arguments": '{"location": "Paris, France", "unit": "celsius"}'
                }
            }
        ]
    },
    {
        "role": "tool",
        "tool_call_id": "call_1",
        "content": '{"temperature": 22, "condition": "sunny", "humidity": 60}'
    },
    {
        "role": "assistant",
        "content": "The weather in Paris is currently sunny with a temperature of 22°C and 60% humidity. It's a beautiful day!"
    }
]

# 使用工具应用对话模板
formatted_with_tools = tokenizer.apply_chat_template(
    messages,
    tools=tools,
    tokenize=False,
    add_generation_prompt=False
)

print("Chat template with tools:")
print(formatted_with_tools)
```

带工具的对话模板输出：

```xml
Chat template with tools:
<|im_start|>system
## Metadata

Knowledge Cutoff Date: June 2025
Today Date: 01 September 2025
Reasoning Mode: /think

## Custom Instructions

You are a helpful assistant with access to tools.

### Tools

You may call one or more functions to assist with the user query.
You are provided with function signatures within <tools></tools> XML tags:

<tools>
{'type': 'function', 'function': {'name': 'get_weather', 'description': 'Get the current weather for a location', 'parameters': {'type': 'object', 'properties': {'location': {'type': 'string', 'description': 'The city and state, e.g. San Francisco, CA'}, 'unit': {'type': 'string', 'enum': ['celsius', 'fahrenheit'], 'description': 'The temperature unit'}}, 'required': ['location']}}}
{'type': 'function', 'function': {'name': 'calculate', 'description': 'Perform mathematical calculations', 'parameters': {'type': 'object', 'properties': {'expression': {'type': 'string', 'description': 'Mathematical expression to evaluate'}}, 'required': ['expression']}}}
</tools>

For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
...
{"temperature": 22, "condition": "sunny", "humidity": 60}<|im_end|>
<|im_start|>assistant
The weather in Paris is currently sunny with a temperature of 22°C and 60% humidity. It's a beautiful day!<|im_end|>
```

### 使用工具使用训练

```python
def format_tool_dataset(examples):
    """格式化带有工具使用的数据集用于训练"""
    formatted_texts = []

    for messages, tools in zip(examples["messages"], examples.get("tools", [None] * len(examples["messages"]))):
        formatted_text = tokenizer.apply_chat_template(
            messages,
            tools=tools,
            tokenize=False,
            add_generation_prompt=False
        )
        formatted_texts.append(formatted_text)

    return {"text": formatted_texts}
```

## 高级模板自定义

对于高级用例，你可能需要更深入地自定义或理解对话模板：

### 检查模型的对话模板

```python
# 查看实际模板
print("SmolLM3 Chat Template:")
print(tokenizer.chat_template)

# 查看使用的特殊标记
print("\nSpecial tokens:")
print(f"BOS: {tokenizer.bos_token}")
print(f"EOS: {tokenizer.eos_token}")
print(f"UNK: {tokenizer.unk_token}")
print(f"PAD: {tokenizer.pad_token}")

# 检查自定义标记
special_tokens = tokenizer.special_tokens_map
for name, token in special_tokens.items():
    print(f"{name}: {token}")
```

### 自定义模板创建

```python
# 创建自定义模板（仅限高级用户）
custom_template = """
{%- for message in messages %}
    {%- if message['role'] == 'system' %}
        {%- set system_message = message['content'] %}
    {%- endif %}
{%- endfor %}
{%- if system_message is defined %}
<|system|>{{ system_message }}<|end|>
{%- endif %}
{%- for message in messages %}
    {%- if message['role'] == 'user' %}
<|user|>{{ message['content'] }}<|end|>
    {%- elif message['role'] == 'assistant' %}
<|assistant|>{{ message['content'] }}<|end|>
    {%- endif %}
{%- endfor %}
{%- if add_generation_prompt %}
<|assistant|>
{%- endif %}
"""

# 应用自定义模板（使用时要非常小心！）
# tokenizer.chat_template = custom_template
```

### 模板调试

```python
def debug_chat_template(messages, tokenizer):
    """调试对话模板应用"""

    # 应用模板
    formatted = tokenizer.apply_chat_template(
        messages,
        tokenize=False,
        add_generation_prompt=True
    )

    # 分词和解码以查看实际词元
    tokens = tokenizer(formatted, return_tensors="pt")

    print("=== TEMPLATE DEBUG ===")
    print(f"Input messages: {len(messages)}")
    print(f"Formatted length: {len(formatted)} chars")
    print(f"Token count: {tokens['input_ids'].shape[1]}")
    print("\nFormatted text:")
    print(repr(formatted))  # 显示转义字符
    print("\nTokens:")
    print(tokens['input_ids'][0].tolist()[:20], "...")  # 前 20 个词元
    print("\nDecoded tokens:")
    for i, token_id in enumerate(tokens['input_ids'][0][:20]):
        token = tokenizer.decode([token_id])
        print(f"{i:2d}: {token_id:5d} -> {repr(token)}")

# 使用示例
debug_messages = [
    {"role": "user", "content": "Hello!"},
    {"role": "assistant", "content": "Hi there!"}
]

debug_chat_template(debug_messages, tokenizer)
```

## 关键要点

理解对话模板对于有效的指令微调至关重要。以下是需要记住的要点：

### 核心概念

1. **模板一致性**：始终为训练和推理使用相同的模板格式 - 不匹配会显著降低性能
2. **生成提示词**：用于推理时使用 `add_generation_prompt=True`，准备训练数据时使用 `False`
3. **角色结构**：清晰的角色定义（`system`、`user`、`assistant`、`tool`）帮助模型理解对话流程
4. **上下文管理**：通过管理对话历史有效地利用 SmolLM3 的扩展上下文窗口
5. **特殊标记处理**：让模板处理特殊标记 - 避免手动添加它们

### 高级功能

6. **双模式推理**：使用 `<|thinking|>` 标签处理需要逐步推理的复杂问题
7. **消息继续**：使用 `continue_final_message=True` 进行结构化输出和预填充响应
8. **工具集成**：现代模板支持函数调用和工具使用以增强能力
9. **Pipeline 自动化**：文本生成 pipeline 在生产环境中自动处理模板
10. **多数据集训练**：在合并训练前标准化不同的数据集格式

### 训练最佳实践

11. **数据集准备**：使用 `add_generation_prompt=False` 和 `add_special_tokens=False` 应用模板进行训练
12. **质量控制**：彻底调试模板以确保正确格式
13. **性能监控**：不正确的模板使用会显著影响模型性能
14. **多模态支持**：模板可通过适当修改扩展到视觉和音频模型

### 需要避免的常见陷阱

- 模板不匹配：使用与模型训练时不同的模板。
- 双重特殊标记：在模板已包含特殊标记时添加特殊标记。
- 缺少系统消息：没有为一致的模型行为提供足够的上下文。
- 格式不一致：在同一数据集中混合不同的对话格式。
- 错误的生成提示词：为你的用例使用不正确的 `add_generation_prompt` 设置。
- 忽略工具语法：未正确格式化工具调用和响应。
- 上下文溢出：未在词元限制内管理长对话。

### 生产考虑

- Pipeline 使用：在生产环境中使用自动化 pipeline 进行一致的模板应用。
- 错误处理：为消息格式和角色序列实现验证。
- 性能优化：尽可能缓存格式化的模板以供重复使用。
- 监控：跟踪模板应用成功率和格式一致性。
- 版本控制：与模型版本一起维护模板版本以实现可重现性。

## 超越基本模板：高级主题

本指南涵盖了基础知识，但对话模板支持许多高级功能：

- 多模态模板：在对话中处理图像、音频和视频。
- 文档集成：包括外部文档和知识库。
- 自定义模板创建：为特定领域应用构建专门的模板。
- 模板优化：高吞吐量应用的性能调优。

有关这些高级主题，请参阅下面链接的专门文档。

## 下一步

现在你已经全面理解了对话模板，你已经准备好学习监督微调，我们将使用这些模板在自定义数据集上训练 SmolLM3。

[下一步：监督微调](./3)

## 综合资源和延伸阅读

### 官方文档
- [对话模板入门](https://huggingface.co/docs/transformers/v4.49.0/chat_template_basics) - 基本概念和使用模式
- [多模态对话模板](https://huggingface.co/docs/transformers/v4.49.0/chat_template_multimodal) - 视觉和音频集成
- [工具和文档](https://huggingface.co/docs/transformers/v4.49.0/chat_template_tools_and_documents) - 函数调用和文档集成
- [高级使用](https://huggingface.co/docs/transformers/v4.49.0/chat_template_advanced) - 自定义模板和优化

### 模型和数据集资源
- [SmolLM3 模型卡](https://huggingface.co/HuggingFaceTB/SmolLM3-3B) - 官方模型文档和使用示例
- [SmolTalk2 数据集](https://huggingface.co/datasets/HuggingFaceTB/smoltalk2) - 用于 SmolLM3 的训练数据集及模板示例
- [TRL 文档](https://huggingface.co/docs/trl) - 带有对话模板集成的训练框架

### 技术参考
- [Transformers 对话模板 API](https://huggingface.co/docs/transformers/main/en/internal/tokenization_utils#transformers.PreTrainedTokenizerBase.apply_chat_template) - 完整 API 参考
- [Jinja2 模板引擎](https://jinja.palletsprojects.com/) - 模板语法和高级功能
- [OpenAI Chat Completions API](https://platform.openai.com/docs/guides/text-generation/chat-completions-api) - 行业标准消息格式

### 社区资源
- [Hugging Face 论坛](https://discuss.huggingface.co/) - 社区讨论和故障排除
- [Discord 服务器](https://discord.gg/UrrTSsSyjb) - 实时帮助和社区互动
- [GitHub Issues](https://github.com/huggingface/transformers/issues) - 错误报告和功能请求
